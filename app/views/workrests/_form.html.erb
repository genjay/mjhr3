<%= simple_form_for(@workrest), :url => worktype_workrest_path(@worktype, @workrest),:html => {:method => :patch} do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :rest_begin_at %>
  	<%= f.input :mins_of_rest %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
