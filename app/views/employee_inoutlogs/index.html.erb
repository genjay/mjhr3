<h2>人員-人員異動設定 (<%= @employee %>)</h2>

<%= form_tag multi_destroy_employee_employee_inoutlogs_path, method: :delete do %>

  <%= link_to '新增 ', new_employee_employee_inoutlog_path, autofocus: true, method: :get, class: 'btn btn-primary' %>
  <%= submit_tag "刪除",class: 'btn btn-danger btn-sm', data: { confirm: '刪除所選資料，Are you sure?' } %>
  <%= link_to '回上一頁', edit_employee_path(@employee), class: 'btn btn-warning', method: :get %>

<table id='table_id'class='table table-hover'>
  <thead>
    <tr>
      <th></th>
      <th>項次</th>
      <th>類別</th> 
      <th>生效日</th>
      <th>部門</th>
      <th>期初年資</th>
    </tr>
  </thead>

  <tbody>
    <% @inoutlogs.each_with_index do |inoutlog,i| %>
      <tr>
        <td><%= check_box_tag "ids[]", inoutlog.id %></td>
        <td><%= i+1 %></td>
        <td><%= link_to inoutlog.to_s, edit_employee_employee_inoutlog_path(inoutlog.employee_id, inoutlog) %></td> 
        <td><%= inoutlog.begin_at %></td>
        <td><%= inoutlog.to_dept_s %></td>
        <td><%= inoutlog.days_of_jobage %></td> 
      </tr>
    <% end %>
  </tbody>
</table>

  <%= link_to '新增 ', new_employee_employee_inoutlog_path, autofocus: true, method: :get, class: 'btn btn-primary' %>
  <%= submit_tag "刪除",class: 'btn btn-danger btn-sm', data: { confirm: '刪除所選資料，Are you sure?' } %>
  <%= link_to '回上一頁', edit_employee_path(@employee), class: 'btn btn-warning', method: :get %>
  
<% end %>