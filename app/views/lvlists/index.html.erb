<h2>Listing Lvlists</h2>

<%= form_tag multi_destroy_lvtype_lvlists_path, method: :delete do %>

  <%= link_to 'New ', new_lvtype_lvlist_path,autofocus: true, method: :get, class: 'btn btn-primary' %>
  <%= submit_tag "Delete Selected",class: 'btn btn-danger', data: { confirm: '刪除所選資料，Are you sure?' } %>
  <%= link_to 'Back ', lvtypes_path, autofocus: true, method: :get, class: 'btn btn-warning' %>

<table id='table_id'class='table table-hover'>
  <thead>
    <tr>
      <th></th>
      <th>項次</th>
      <th>Amt</th>
    </tr>
  </thead>

  <tbody>
    <% @lvlists.each_with_index do |lvlist,i| %>
      <tr>
        <td><%= check_box_tag "ids[]", lvlist.id %></td>
        <td><%= i+1 %></td>
        <td><%= link_to lvlist.amt, edit_lvtype_lvlist_path(lvlist.lvtype_id, lvlist) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

  <%= link_to 'New ', new_lvtype_lvlist_path,autofocus: true, method: :get, class: 'btn btn-primary' %>
  <%= submit_tag "Delete Selected",class: 'btn btn-danger', data: { confirm: '刪除所選資料，Are you sure?' } %>
  <%= link_to 'Back ', lvtypes_path, autofocus: true, method: :get, class: 'btn btn-warning' %>
  
<% end %>
